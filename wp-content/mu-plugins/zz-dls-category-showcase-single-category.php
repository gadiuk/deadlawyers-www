<?php
/**
 * Plugin Name: DLS Category Template (Temporarily Disabled)
 * Description: Disabled after a live fatal error so category archives fall back to the Kadence default template.
 * Version: 3.0.2
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

// Intentionally inactive. Category archives use the theme's native archive flow.
